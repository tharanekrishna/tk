#include<stdio.h>
#include<math.h>
#include<conio.h>
int main()
{
int num,tem,rem,sum=0;
clrscr();
printf("\n enter the number");
scanf("%d",&num);
tem=sum;
while(num!=0)
for(i=0;i<=num;i++)
sum=sum+(rem*rem*rem);
sum=(num/10,num/10);
printf("% it is an amstrong intervel",tem);
getch();
}
